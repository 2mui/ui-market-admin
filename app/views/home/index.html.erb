<% if current_user %>
    <div class="max-w-lg mx-auto mt-16 text-center">
    <% if current_user.editor? %>
      <p>Hi, <%= current_user.email %>,  your role is <%= current_user.role %></p>
    <% else %>
      <p>Hi, <%= current_user.email %>,  your role is <%= current_user.role %></p>
    <% end%>
    <div>

    <div class="max-w-lg mx-auto mt-16 text-center">
      <h1 class="mb-4 text-3xl font-black text-blue-600">
        <a href="/admin">Go to Admin Panel</a>
      </h1>

      <% if current_user.admin? %>
        <h1 class="mb-4 text-3xl font-black text-blue-600">
          <a href="/sidekiq" target="_blank">Sidekiq</a>
        </h1>

        <h1 class="mb-4 text-3xl font-black text-blue-600">
          <a href="/pghero" target="_blank">PgHero</a>
        </h1>
      <% end %>

      <h1 class="mb-4 text-3xl font-black text-blue-600">
        <a href="https://www.2mui.cn" target="_blank">Frontend</a>
      </h1>
    </div>
<% else %>
    <h1 class="mb-4 text-3xl font-black text-blue-600">
      <a href="/admin">Login</a>
    </h1>
<% end %>

